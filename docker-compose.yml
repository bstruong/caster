services:
  postgres:
    image: postgres:16
    container_name: caster-postgres
    ports:
      - "5435:5432"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: caster_development
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
